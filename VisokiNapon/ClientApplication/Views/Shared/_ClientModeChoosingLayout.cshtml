<!DOCTYPE html>
<html>
<head>
    <title>Visoki napon</title>
    <style type="text/css">

        html {
            background: url("Images/purple-initial-gui.png") no-repeat center center fixed;
            -webkit-background-size: cover;
            -moz-background-size: cover;
            -o-background-size: cover;
            background-size: cover;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
        }

        img {
            display: block;
            margin-left: auto;
            margin-right: auto;
            margin-top: 5%;
        }

        input[type=text], select {
            width: 100%;
            padding: 12px 20px;
            margin: 8px 0;
            display: inline-block;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        button {
            height: 170px;
            width: 449px;
            background-color: #5d3c5d;
            color: white;
            font-size: 19px;
            padding: 14px 40px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

            button:hover {
                background-color: #4e324e;
            }

        .disabled {
            opacity: 0.8;
            cursor: not-allowed;
        }

        .container {
            position: absolute;
            top: 65%;
            left: 17%;
        }
    </style>
</head>
<body>
    @RenderBody()
</body>
</html>
